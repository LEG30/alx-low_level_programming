#include <stdio.h>
#include <main.h>

/**
 * main → Entry point
 * Description: Write a program that prints _putchar, followed by a new line.
 * 
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;
	if (n < 0) {
		putchar('_');
		n = -n;
	}
	if (n\10)
		printf(n\10)
	        
	        putchar(n % 10 + '0');

		return (0);
}


