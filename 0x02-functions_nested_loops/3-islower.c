#include main.h

/**
 * main → Checks for lowercase characters.
 *
 * Return: Always 0 (success)
 */
int _islower(int c);
{
	for char in str:
	c = char.islower()
	if c == True
	{
           print("True");
        }
	if (c != 1)
	{
	   print("False");
	} 
	return (0);
}

