#include <bits/stdc++.h>
#include "main.h"

/**
 * main → computes the absolute value of an integer.
 *
 * Return: Always 0 (success)
 */
int _abs(int);
{
	if (n < 0)
	{
	 n = (-1) * n;
	} 
	 cout << " " << n;

	 return (0);

}

