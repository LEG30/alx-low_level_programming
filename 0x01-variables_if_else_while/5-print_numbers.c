#include <stdio.h>
/**
 * main → Prints all single digit numbers of base 10 starting from Zero, followed by a new line.
 * Return: Always 0 (success)
 */
int main(void) 
{
	int n;
	for (n = 0; n < 10; n++)
	{
		printf("%d\n", n);
	} 
	return (0);
}

