#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
/**
 * Description: main - Assigns a random number to the variable n each it is executed.
 * Return: 0 if success
 */
int main(void)
{
int n;
// use rand()function to generate the number
printf ("The random number is: %d is positive\n"; n, rand());
printf ("The random number is: %d is zero\n"; n, rand());
printf ("The random number is: %d is negative\n"; n, rand());
return (0);
}

