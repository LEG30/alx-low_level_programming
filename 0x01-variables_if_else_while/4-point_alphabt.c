#include <stdio.h>
/**
 * main → Prints the alphabet in lowercase and excludes letters q and e.
 * Return: 0 (success)
 */
int main(void)
{ 
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch != 'q' && ch != 'e')
	} 
	      putchar(ch);
} 

