#include <stdio.h>
/**
 * main → Prints all single base numbers 0f base 10 starting from .
 * Return: Always 0
 */
int main(void)
{
	int(ch);
	for (ch = 48; ch <= 57, ch++)
	{
		putchar (ch);

		putchar (10): /* this is an ascii code for the new line */

		return (0);
}

